#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#include<bits/stdc++.h>

using namespace std;

#define              MAX             500005
#define              INF             1<<30
#define              EPS             1e-9
#define pi 2*acos(0.0)
/*
struct node{
    int vrtx;
    int wght;
    node(int _vrtx,int _wght){
        vrtx=_vrtx;
        wght=_wght;
    }
    bool operator < (const node& p) const {   return wght >p.wght;}
};
*/
/*
struct data {
    char name[100];
    int height, weight;
    long long income;
    bool operator < ( const data& b ) const {
        if( income == b.income ) {
            if( height == b.height ) {
                if( weight == b.weight )
                    return strlen( name ) < strlen( b.name );//smallest age chaile '<'
                else return weight < b.weight;
            }else return height > b.height;
        }else return income > b.income; //biggest age chaile >
    }
};
*/


int main(){
    //freopen("input.txt", "r", stdin);



    //fclose(stdin);

    return 0;
}
